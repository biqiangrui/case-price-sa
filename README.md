# CasePriceSA

Static phone-case price search prototype.

## Deploy

Deploy this `site` folder to Vercel as a static site. No build command is needed.

## Data

The site reads product data from Supabase using `supabase-config.js`.
If Supabase is unavailable, `products.csv` remains as a local fallback.
